apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: projet-eni
  name: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
        - name: backend
          image: 1tanguy/projet-devops:backend-2d50d4c
          ports:
            - containerPort: 3000
          envFrom:
            - configMapRef:
                name: todolist-config
            - secretRef:
                name: todolist-secrets
          env:
            - name: DB_NAME
              valueFrom:
                configMapKeyRef:
                  name: todolist-config
                  key: DB_NAME
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: todolist-secrets
                  key: DB_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: todolist-secrets
                  key: DB_PASSWORD
            - name: DB_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: todolist-secrets
                  key: DB_ROOT_PASSWORD
      tolerations:           
        - key: "CriticalAddonsOnly"
          operator: "Exists"
          effect: "NoSchedule"